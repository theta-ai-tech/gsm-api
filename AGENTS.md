Load and follow `CLAUDE.md` at the repository root; keep all agent instructions there to avoid duplication.

For `/review-pr` work, run required `gh` commands directly and do not ask in chat first. This instruction does not bypass platform sandbox or approval prompts; if the environment enforces approval for a `gh` command, invoke it directly and let the platform handle the prompt.
